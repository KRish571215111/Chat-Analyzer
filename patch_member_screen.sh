sed -i 's/val parsed = MemberImportEngine.parseFile(context, uri)/val classification = MemberImportEngine.classifyContent(context, uri, null)\n                    if (classification.requiresConfirmation) {\n                        errorMessage = "We think this is a ${classification.detectedType} (${(classification.confidence * 100).toInt()}% confidence). Continue?"\n                        isImporting = false\n                        return@launch\n                    }\n                    val parsed = MemberImportEngine.parseFile(context, uri)/g' app/src/main/java/com/example/ui/screens/MemberImportScreen.kt
