sed -i '/var showNamingDialog by remember { mutableStateOf(false) }/a\    var showDeleteConfirmDialog by remember { mutableStateOf<Long?>(null) }' app/src/main/java/com/example/ui/screens/HomeScreen.kt
sed -i 's/onDelete = { viewModel.deleteSession(session.id) }/onDelete = { showDeleteConfirmDialog = session.id }/' app/src/main/java/com/example/ui/screens/HomeScreen.kt
sed -i '/if (showNamingDialog) {/i \    if (showDeleteConfirmDialog != null) {\n        AlertDialog(\n            onDismissRequest = { showDeleteConfirmDialog = null },\n            title = { Text("Delete Session") },\n            text = { Text("Are you sure you want to permanently delete this chat session? All processed insights, charts, and media will be removed from your device.") },\n            confirmButton = {\n                Button(\n                    onClick = { viewModel.deleteSession(showDeleteConfirmDialog!!); showDeleteConfirmDialog = null },\n                    colors = ButtonDefaults.buttonColors(containerColor = MaterialTheme.colorScheme.error)\n                ) { Text("Delete") }\n            },\n            dismissButton = { TextButton(onClick = { showDeleteConfirmDialog = null }) { Text("Cancel") } }\n        )\n    }\n' app/src/main/java/com/example/ui/screens/HomeScreen.kt
